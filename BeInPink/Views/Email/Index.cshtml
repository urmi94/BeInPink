@model IEnumerable<BeInPink.Models.SendEmailViewModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Subject)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Contents)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Subject)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Contents)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ToEmail }) |
            @Html.ActionLink("Details", "Details", new { id=item.ToEmail }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ToEmail })
        </td>
    </tr>
}

</table>
